name: CI & Deploy to Infomaniak
on:
    push:
        branches: ["main"]
jobs:
    build-deploy:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup environment
              run: |
                  echo "APP_ENV=test" > .env.test.local
                  echo "DATABASE_URL=sqlite:///:memory:" >> .env.test.local
                  echo "APP_SECRET=test" >> .env.test.local

            - name: Setup PHP
              uses: shivammathur/setup-php@v2
              with:
                  php-version: "8.2"
                  extensions: mbstring, intl, pdo_mysql, opcache
                  coverage: none

            - name: Install dependencies
              run: |
                  echo "📦 Installation des dépendances..."
                  composer install --no-interaction --prefer-dist --no-scripts
                  echo "✅ Dépendances installées"

            - name: Prepare production build
              run: |
                  echo "🏗️ Préparation du build production..."
                  composer install --no-dev --prefer-dist --no-interaction --optimize-autoloader --no-scripts
                  echo "✅ Build production prêt"

            # --- MODIFICATION 1 ---
            - name: Create release archive
              run: |
                  echo "📦 Création de l'archive de déploiement..."
                  # Crée l'archive dans un chemin absolu (/tmp) pour éviter les problèmes de chemins relatifs
                  tar -czf /tmp/release.tar.gz \
                    --exclude='.git' \
                    --exclude='.github' \
                    --exclude='tests' \
                    --exclude='var/cache/*' \
                    --exclude='var/log/*' \
                    --exclude='.env.test*' \
                    --exclude='phpunit.xml*' \
                    --exclude='node_modules' \
                    .
                  echo "✅ Archive créée: /tmp/release.tar.gz"

            - name: Add host key
              run: |
                  mkdir -p ~/.ssh
                  ssh-keyscan -p "${{ secrets.SSH_PORT }}" "${{ secrets.SSH_HOST }}" >> ~/.ssh/known_hosts

            # --- MODIFICATION 2 ---
            - name: Upload archive to server
              uses: appleboy/scp-action@v0.1.7
              with:
                  host: ${{ secrets.SSH_HOST }}
                  username: ${{ secrets.SSH_USER }}
                  key: ${{ secrets.SSH_KEY }}
                  port: ${{ secrets.SSH_PORT }}
                  # Le chemin source est mis à jour pour pointer vers le chemin absolu de l'archive
                  source: "/tmp/release.tar.gz"
                  target: "~/deploy"

            - name: Deploy on server (extract, link, composer, migrate)
              uses: appleboy/ssh-action@v1.2.0
              with:
                  host: ${{ secrets.SSH_HOST }}
                  username: ${{ secrets.SSH_USER }}
                  key: ${{ secrets.SSH_KEY }}
                  port: ${{ secrets.SSH_PORT }}
                  script: |
                      set -euo pipefail
                      DEPLOY_PATH=${{ secrets.DEPLOY_PATH }}
                      RELEASES="$DEPLOY_PATH/releases"
                      SHARED="$DEPLOY_PATH/shared"
                      TS=$(date +%Y%m%d%H%M%S)

                      echo "🚀 Début du déploiement..."
                      mkdir -p "$RELEASES/$TS"
                      tar -xzf "$HOME/deploy/release.tar.gz" -C "$RELEASES/$TS"
                      cd "$RELEASES/$TS"

                      # Symlinks vers le shared (persistant)
                      ln -sfn "$SHARED/.env.local" .env.local || true
                      mkdir -p "$SHARED/var" "$SHARED/public/uploads"
                      rm -rf var public/uploads
                      ln -sfn "$SHARED/var" var
                      ln -sfn "$SHARED/public/uploads" public/uploads

                      # Dépendances côté serveur (avec la vraie DB)
                      echo "📦 Installation dépendances production..."
                      composer install --no-dev --prefer-dist --no-interaction --optimize-autoloader

                      # Migrations DB (maintenant qu'on a la vraie config)
                      if [ -f bin/console ]; then
                        echo "🗄️ Migration base de données..."
                        php bin/console doctrine:migrations:migrate --no-interaction --allow-no-migration
                        
                        echo "⚡ Optimisation cache..."
                        php bin/console cache:clear --env=prod --no-warmup
                        php bin/console cache:warmup --env=prod
                        
                        # API Platform - génération doc
                        echo "📚 Génération documentation API..."
                        php bin/console api:openapi:export --yaml > public/api-docs.yaml || echo "Pas de doc générée"
                      fi

                      # Bascule atomique
                      echo "🔄 Bascule vers nouvelle version..."
                      ln -sfn "$RELEASES/$TS" "$DEPLOY_PATH/current"

                      # Droits d'écriture
                      chmod -R u+rwX,g+rwX var || true

                      echo "✅ Déploiement terminé avec succès!"

                      # Health check optionnel
                      sleep 2
                      echo "🏥 Test de santé de l'API..."
                      # curl -f https://ton-domaine.com/api || echo "⚠️ Health check à vérifier manuellement"
